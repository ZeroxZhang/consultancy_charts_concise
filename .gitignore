# macOS
.DS_Store

# Node / Python
node_modules/
.font-venv/
__pycache__/
.cache/
.tmp-cache/

# Logs
*.log

# Agent 会话记录与本地配置。
# 曾有一次 git add 把工作区整个扫进去，这些从未提交，但对象一直留在 .git 里，
# 实测不可达的 281MB 中它们占 88MB。这里挡住，避免再被扫进去。
/.claude/
*.jsonl

# ---------------------------------------------------------------------------
# 仅本地保留，不对外呈现。
# 开发过程记录与历史研究（迭代、复核、审计、设计底稿、素材）统一收在 _local/ 下，
# 本地完整保留，只是不进公开仓库。原来逐类列在根目录，一是根目录杂，
# 二是会留下像 /research_mock/ 这种没有对应目录的失效规则。
# ---------------------------------------------------------------------------
/_local/

# 个人 Agent 配置（含本机路径与个人工作约定）
/AGENTS.md
/CLAUDE.md

# 生成的可视化样张（可由 scripts/build_*.cjs 重建）
/demo/

# macOS
.DS_Store

# Node / Python
node_modules/
.font-venv/
__pycache__/
.cache/
.tmp-cache/

# Logs
*.log

# ---------------------------------------------------------------------------
# 仅本地保留，不对外呈现。
# 开发过程记录与历史研究（迭代、复核、审计、设计底稿、素材）统一收在 _local/ 下，
# 本地完整保留，只是不进公开仓库。原来逐类列在根目录，一是根目录杂，
# 二是会留下像 /research_mock/ 这种没有对应目录的失效规则。
# ---------------------------------------------------------------------------
/_local/

# 个人 Agent 配置（含本机路径与个人工作约定）
/AGENTS.md
/CLAUDE.md

# 生成的可视化样张（可由 scripts/build_*.cjs 重建）
/demo/

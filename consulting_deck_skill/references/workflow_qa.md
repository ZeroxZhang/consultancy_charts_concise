# 咨询 deck 验收 · v9

> v9适用原则：下列字段、编号、评分和布局预算用于复杂任务的按需参考，可合并或省略不影响结果的过程记录。分析/编码正确、核心内容可读完整与同版交付是底线；默认阈值和作者选择的区别见 [开放创作](open_authoring.md)。

格式正确不等于咨询质量。报告分开写“分析/证据/视觉/工程”状态；某项未执行即未验收。

## 分析前置与最终内容复核

S2按实际风险选用 [analysis_review.md](analysis_review.md) 的检查项；复杂分析使用独立分析审查，不能等页面做完才检查推理。S7读取实际保留的分析/来源底稿及最终标题，重点核对方法前提、关键反证、目标/预测与建议条件有无丢失。
editorial用原文定位与必要核对记录执行内容复核，不为成稿QA补建Q/T/F/E台账。
分析ready只允许当前有限结论进入制作，不代表事实外部核验、视觉或工程已经通过。最终Blocking/Major仍为零才称通过。

## 既有检查编号（保持引用兼容）

E-01 标题连读形成论证。E-02 章节按决策所需顺序。E-03 页码/导航统一。
E-04 同实体同术语同编码。E-05 正文足以支持标题、结论不超出证据。
E-06 一页一个主判断，可用多个共同证据；不按标题逗号个数拆页。
E-07 删除本页会失去哪项必要证据或决策信息？没有损失才合并/删除。
E-08 数字一致与计算闭合。E-09 期间/单位/分母/范围可比。
E-10 数据逐项可追溯原文，非仅有一个Source行。E-11 事实/估计/示意/建议区分。
E-12 视觉规范实际应用。E-13 几何映射、标签、图例与轴正确。
E-14 颜色有冗余标签。E-15 状态角标准确。E-16 文字与术语校对。

## 35条执行规则（编号兼容，不冒称MBB官方铁律）

E-R01 标题写判断。
E-R02 一个主判断允许多个证据模块；独立判断按逻辑拆分。
E-R03 标题连读形成故事。
E-R04 图/表/机制必须履行标题的证明责任。
E-R05 长度编码条形从零开始；非零范围应改点图/区间图，不以截断条夸大。
E-R06 条形按比较任务排序，自然顺序除外。
E-R07 饼图用于单一构成，精细比较优先条形；不设全篇占比配额。
E-R08 饼片顺序、总和与直接标签清楚。
E-R09 多线拥挤则分面，实体色与线型稳定。
E-R10 时点少仍可画趋势线，依据要表达的关系而非固定点数决定。
E-R11 颜色按品牌/分类/量值/偏差/状态/强调分工，不设全篇单强调色硬约束。
E-R12 同实体编码一致，换编码显式说明。
E-R13 二维数据不以3D装饰扭曲。
E-R14 双轴谨慎，优先分面或指数化并写基期。
E-R15 关键标签静态可见，图例靠近对应图。
E-R16 单位、期间、口径可见。
E-R17 预测/估计明确方法、区间、线型。
E-R18 数字回溯证据项。
E-R19 瀑布起止锚零、累计闭合、跨零正确；增减与好坏分开。
E-R20 Mekko面积按值、宽度按总量、段高按列内比例。
E-R21 表格文本左/数值右，重点选择性强调。
E-R22 流程表达顺序/责任/分支/依赖，图形由关系决定。
E-R23 密度按媒介与证明责任，取消统一五条要点限制。
E-R24 标题通常两行；特殊结构可更多，按实际空间与阅读层级判断，不机械截字。
E-R25 状态角标反映真实状态。
E-R26 状态色同时有文字/符号。
E-R27 删除无意义的装饰、复述、填充卡片。
E-R28 正文保留足够证据，方法明细进附录并回链。
E-R29 同一知识点避免重复全量。
E-R30 灰度和打印实际查看。
E-R31 大量原始材料已建立清单，图中数值/文本可定位回文件、表/页和原子证据。
E-R32 主要展品的输入关系、读者任务和可比性清楚；复杂页可记录I/A编号与render_route，简单页不补登记。
E-R33 选图所需的分母、原始分布、节点边、层级、日期或流量真实存在，不能从叙述猜造。
E-R34 超出某实现容量时，调整布局、聚合、分面、换实现或分页；保持实际可读，重要异常和明细有去向，不把未知填0。
E-R35 ECharts配方、图内注释和标题中的派生数来自同一输入，不分别硬编码。

## 逐页视觉评分：0–4分，使用截图证据

| 维度 | 0–1分：返工 | 2分：尚不足 | 3分：可交付 | 4分：强表现 |
|---|---|---|---|---|
| 证据充分性 | 缺证据/错误比较 | 数据在但推理缺环 | 判断有充分证据与边界 | 比较、反例和决策条件完整 |
| 阅读层级 | 无视觉主次 | 要点并列但关系弱 | 标题→子结论→证据可扫 | 快读与细读都清楚 |
| 布局重心 | 截断/巨大空框 | 重心失衡或空洞 | 空白用于分组、对齐明确 | 模块尺寸贴合信息、关系自然 |
| 配色编码 | 冲突/虚假精度 | 可读但解释成本高 | 角色一致、几何合法 | 关键比较一眼可辨 |
| 展品完成度 | 空图/标签碰撞 | 库默认样式/注释不足 | 单位标签注释完整 | 复杂分析仍低阅读成本 |

评分用于定位设计差距，可选而非交付硬门槛。实际是否满足读者目标、有没有影响理解或决策的缺陷决定通过；风格偏好不自动构成Major。
分数不可代替具体缺陷；有问题时记录页码、所见、影响和修复依据即可。

## 工程证据

- H-01 single-file入口与offline自包含区分；离线关键证据完整，不能仅提示“图表不可用”。
- H-02 实际翻页、#3、G/Esc、全屏、缩放、Home/End；不只读代码。
- H-03 每页截屏、逐元素越界；检查SVG文本碰撞/截断；SVG与canvas尺寸区别对待。
- H-04 捕获console/pageerror；图表可见时初始化，打印前全部实例完成。
- H-05 普通文字对比度≥4.5；来源也是文字，不豁免。
- H-06 PDF页数=deck页数且关键图和标签确实进入打印，不把文件存在视为成功。
- H-07 ECharts版本与`package.json`一致；改动配方代码才重跑对应测试。offline交付中的ECharts图已静态化或依赖已内联，断网不缺主证据。
- H-08 typography_id/version与简报一致；CSS/SVG/ECharts/SSR同源配置；字体就绪后测量，实际平台字体来自指定资源，无意外系统回退与伪粗。legacy-system单列兼容状态，不等同新字体通过。
- H-09 PDF字体嵌入并可检索，冷缓存断网后字体与逐页几何一致。静态SVG不自动等于字体自包含；子集覆盖隐藏页、动态标签、CSS大小写、货币/负号与最终修改字符。
- H-10 字体角色遵循所选配置；serif-report/serif-playfair主标题衬线，sans-presentation主标题无衬线；数字对齐覆盖最终SVG/PDF。标题行数和推荐字号按实际媒介判断，解释性备注不得冒充12px来源。
- H-11 S8交付HTML与独立PDF页数一致；HTML内嵌PDF与独立文件SHA-256一致，断网可下载。下载不改变页码/hash/总览状态，导出控件不进入打印；定稿HTML修改后必须重新生成、验收并打包PDF。

使用 `scripts/qa_deck.cjs deck.html output-dir` 产生自动检查与逐页截图。
自动PASS只能报告几何/运行通过；每页原尺寸图片必须通过图片工具实际查看。
灰度、PDF和总览也要检查。工具不能看图时视觉状态写“未验收”，禁止“像素统计更强”的说法。

## 问题级别

Blocking：数据或编码误导、核心证据缺失、无法显示/打印、关键文字被遮挡。
Major：证据链、层级、色义或图注缺陷显著影响理解或决策。留白、图型或字体偏好本身不构成Major。
Minor：不影响理解的小间距/措辞瑕疵。未执行的检查另列，不归Minor糊弄通过。

## 旧流行规则登记 M-01…M-10

M-01 不禁饼图、也不强制配额。M-02 无统一白底+单色禁令，白底是本项目默认。
M-03 双轴非绝对禁用。M-04 二维定量不装饰3D。M-05 一页一观点不等于一图。
M-06 类别颜色无统一三色上限。M-07 瀑布不依赖特定工具。M-08 字体借鉴非内部规范。
M-09 饼片数取决可读性，常见3–5为建议。M-10 允许流程图，重点检查关系是否有信息。

## v3主题一致性契约

接收并继承brief中的theme_id/theme_version/selection_basis/overrides和实体→cat索引登记，不自行重选主题。
CSS、ECharts series/visualMap、SVG、表格、状态列必须来自同一assets/deck-themes.js预设。
检查数学正负不误用经营好坏；BCG品牌绿不自动表示达标；Accenture渐变不编码类别。
验收实际fill/stroke、文字反差≥4.5:1、灰度直接标签、打印色阶及图例上下限。
切换主题后必须重建静态图并产生新截图/PDF；仅CSS换色不算通过。

## v5高密度与分析表达验收（补充E-08/E-13/E-R15/E-R21/E-R22/E-R31…35）

- 遮掉外围解释后，图或表能否支持其子判断？新增括号、数值列、颜色或箭头各减少了什么理解成本？
- 差异端点绑定源值；百分比、百分点、CAGR的分母与间隔正确；零/负基数不能硬算常规增长率。
- 堆积图总量与份额区分；百分轴Mekko总量对应列宽。零值不画假面积，小片标签可从静态表格查到。
- 表格单位、数值对齐、统一数据条量尺、缺失和总计语义明确；不同口径不能因外观一致而排名。
- 图示的责任、交付、条件与关系可读；顺序不是因果，等宽阶段不是等时长。
- 分别检查原尺寸SVG文字碰撞、图内和图外越界、灰度、PDF。估宽通过或自动几何PASS不代替这些检查。
- 保留普通表格/条形的页面可以获得最高完成度评价；不按复杂程度或think-cell相似度打分。
- 从`source_inventory`抽查至少一个大表、一个长文本来源和一个派生指标，确认没有整表/整段作为单一证据，也没有从未编码文本生成伪精确统计。
- 复杂页若已有page_plan，对照关键编码与实际图型；改变影响理解的设计取舍时更新相关笔记，不补造候选评分与回退文件。
- ECharts 6升级后检查默认布局变化、轴标签防溢出、富文本继承、图例位置和SVG输出；自定义主题不能替代这些回归。
- 修改ChartRuntime或配方时，验证其prepare/render/check正式入口并覆盖受影响边界，例如小画布分页、零值、负值、深树或Sankey闭合。自定义SVG或原生option按自己的实现与实际成稿检查，不强制接入这套API。
- `test_theme_browser.cjs`比较三主题的真实浏览器与SSR文字，不能只检查CSS token；`test_dense_inputs.cjs`检查原始明细→派生→选型与原文定位，并用坏单位/重复主键/虚假引文验证拒绝路径。
- `qa_deck.cjs`检查SVG的有效字号（包括viewBox/transform缩放）、单元格溢出、PDF页数/标题和离线逐页文字/标签一致性；仍需目视PDF图形和截图，不能把标题存在当作全部打印证据完整。

## v9检查与放行

常规制稿不需重跑全部库回归；按 [开放创作](open_authoring.md) 选择检查。qa_deck的warnings提示专业判断，不要求机械消除；errors表示已检测的客观失败。来源/数据极小文字、打印丢展品和版本不同仍阻断。S8用 [交付系统](delivery_system.md) 的单份review.json记录实际内容/证据/视觉复核，不另建繁重审批。
